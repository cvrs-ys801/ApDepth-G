base_config:
- config/logging.yaml
- config/wandb.yaml
# - config/dataset/data_vkitti_train.yaml
# - config/dataset/data_vkitti_val.yaml
- config/dataset/dataset_train.yaml
- config/dataset/dataset_val.yaml
# 下面这个可视化可以不要
# - config/dataset/dataset_vis.yaml
- config/model_sdv2.yaml


pipeline:
  name: MarigoldPipeline
  kwargs:
    scale_invariant: true
    shift_invariant: true

depth_normalization:
  type: scale_shift_depth
  clip: true
  norm_min: -1.0
  norm_max: 1.0
  min_max_quantile: 0.02

augmentation:
  lr_flip_p: 0.5

dataloader:
  num_workers: 12
  effective_batch_size: 36
  max_train_batch_size: 6
  seed: 2024  # to ensure continuity when resuming from checkpoint

# Training settings
trainer:
  name: MarigoldTrainer
  training_noise_scheduler:
    pretrained_path: stable-diffusion-2
  init_seed: 2024  # use null to train w/o seeding
  save_period: 250
  backup_period: 2000
  validation_period: 30000
  visualization_period: 40000

multi_res_noise:
  strength: 0.9
  annealed: true
  downscale_strategy: original

gt_depth_type: depth_raw_norm
gt_mask_type: valid_mask_raw

# 建议这个数字不要再动
max_epoch: 10000  # a large enough number to ensure convergence
max_iter: 23000 # usually converges at around 20k

optimizer:
  name: Adam

loss:
  name: mse_loss
  kwargs:
    reduction: mean

lr: 3.0e-05
lr_scheduler:
  name: IterExponential
  kwargs:
    total_iter: 25000
    final_ratio: 0.01
    warmup_steps: 100

# Validation (and visualization) settings
validation:
  denoising_steps: 50
  ensemble_size: 1  # simplified setting for on-training validation
  processing_res: 0
  match_input_res: false
  resample_method: bilinear
  main_val_metric: abs_relative_difference
  main_val_metric_goal: minimize
  init_seed: 2024

eval:
  alignment: least_square
  align_max_res: null
  eval_metrics:
  - abs_relative_difference
  - squared_relative_difference
  - rmse_linear
  - rmse_log
  - log10
  - delta1_acc
  - delta2_acc
  - delta3_acc
  - i_rmse
  - silog_rmse

normal_loss:
  weight: 0.5

# Ambiguity-aware Far-field Geometry (AFG)
# Keeps the UNet input strictly 12-channel and does not use explicit sky masks.
# It builds a soft ambiguous far-region weight from DA2 prior + RGB low-texture cues,
# then applies far-field smoothness, regional prior anchoring, and optional inference guidance.
afg:
  enabled: true
  far_is_larger: true      # set false if your DA2 wrapper outputs inverse-depth-like maps
  far_gamma: 2.0           # larger => focuses more strongly on the farthest DA2 regions
  texture_alpha: 8.0       # larger => focuses more strongly on low-texture regions
  smooth_weight: 0.03      # latent smoothness in far/low-texture regions
  anchor_weight: 0.02      # regional mean anchor to DA2 prior, avoids local pseudo-texture copying
  latent_grad_weight: 0.1  # existing latent gradient consistency weight
  guidance_weight: 0.03    # inference-time weak far-field prior attraction; use 0.0 to disable
